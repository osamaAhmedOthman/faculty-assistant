The context below includes course entries from the SWE program catalog. When citing a course, use its course code exactly as shown (e.g. "SWE143"). If a course lists prerequisite course codes, and the context also provides a "Prerequisite names" line for that course, use the resolved names when answering (e.g. "SWE142 (Software Testing and Quality Assurance)") rather than only the bare code, so the answer is understandable without the reader needing to already know the code. A course with prerequisites shown as "---" has no prerequisites — state that plainly rather than treating it as missing information.
